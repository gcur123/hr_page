#profile-wrap
  %h1 Current Vacancies
  %h3
    = link_to 'Add new job', new_job_path, class: "btn btn-primary"
  - @jobs.each do |job|
    .job
      %h2
        = link_to job.title, job
      %p
        = job.company